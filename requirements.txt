# COMTRADE波形可视化工具 - Python依赖包

# 核心依赖
plotly>=5.0.0          # 交互式图表库
numpy>=1.19.0          # 数值计算库

# 可选依赖（用于扩展功能）
# pandas>=1.3.0        # 数据处理（用于Excel导出）
# openpyxl>=3.0.0      # Excel文件支持
# matplotlib>=3.3.0    # 静态图表（用于自定义可视化器）
# scipy>=1.7.0         # 科学计算（用于FFT分析等）
